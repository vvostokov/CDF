# System Context — Diagrams

> Companion diagrams for `.cognition/architect/system_context.md`.

## Diagrams

* `system-context.svg` — C4 Level 1 (system in its environment).
* `containers.svg` — C4 Level 2 (containers / processes).
* `deployment.svg` — C4 Level 3 (deployment topology).

## How to Use

1. Open the SVG in your browser or Markdown viewer.
2. Read the matching text in `context/system-context.md` (created per project).
3. Cross-reference with `.cognition/architect/system_context.md`.

## How to Update

1. Edit the source in `architecture/diagrams/`.
2. Re-render (or let CI render).
3. Update the matching `.md` in `context/`.

---

> *Diagrams are how the architecture meets the eye.*